use nix
# Re-evaluate the dev shell when its inputs change. The shell hook copies the
# generated workflow YAML into .forgejo/, so a stale cached environment
# silently rewrites the YAML with outdated content on every load.
watch_file lon.lock ci.nix shell.nix workflows/*.nix
